const state = {
  department: []
}

const getters = {
  getDepartment: state => {
    return state.department;
  }
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}